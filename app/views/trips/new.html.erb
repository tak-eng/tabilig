<div class="contents row">
  <div class="container">
    <h3>旅行先は？</h3>
    <%= form_with(model: @tweet, local: true) do |form| %>
      <%= form.text_field :name, placeholder: "旅行先" %>
      <%= form.submit "SEND" %>
    <% end %>
  </div>
</div>